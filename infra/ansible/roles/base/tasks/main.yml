---
- name: Sicherstellen, dass mindestens ein SSH-Key für den Admin-User vorhanden ist
  ansible.builtin.assert:
    that:
      - admin_user_authorized_keys | length > 0
    fail_msg: "Setze admin_user_authorized_keys, bevor das Basis-Setup ausgeführt wird."

- name: Sicherstellen, dass mindestens ein SSH-Key für den Deploy-User vorhanden ist
  ansible.builtin.assert:
    that:
      - deploy_user_authorized_keys | length > 0
    fail_msg: "Setze deploy_user_authorized_keys, bevor das Basis-Setup ausgeführt wird."

- name: Zeitzone setzen
  community.general.timezone:
    name: "{{ timezone }}"

- name: Basis-Pakete installieren
  ansible.builtin.apt:
    name: "{{ base_packages }}"
    state: present
    update_cache: true

- name: Admin-User anlegen
  ansible.builtin.user:
    name: "{{ admin_user }}"
    shell: "{{ admin_user_shell }}"
    groups: "{{ admin_user_groups }}"
    append: true
    create_home: true

- name: SSH-Keys für Admin-User hinterlegen
  ansible.posix.authorized_key:
    user: "{{ admin_user }}"
    key: "{{ admin_user_authorized_keys | join('\n') }}"
    state: present
    exclusive: true

- name: Passwortloses sudo für Admin-User erlauben
  ansible.builtin.copy:
    dest: "/etc/sudoers.d/90-{{ admin_user }}"
    owner: root
    group: root
    mode: "0440"
    content: "{{ admin_user }} ALL=(ALL) NOPASSWD:ALL\n"
    validate: "/usr/sbin/visudo -cf %s"
  when: admin_user_sudo_nopasswd

- name: Benötigte Gruppen für Deploy-User sicherstellen
  ansible.builtin.group:
    name: "{{ item }}"
    state: present
  loop: "{{ deploy_user_groups }}"

- name: Deploy-User anlegen
  ansible.builtin.user:
    name: "{{ deploy_user }}"
    shell: "{{ deploy_user_shell }}"
    groups: "{{ deploy_user_groups }}"
    append: false
    create_home: true

- name: SSH-Keys für Deploy-User hinterlegen
  ansible.posix.authorized_key:
    user: "{{ deploy_user }}"
    key: "{{ deploy_user_authorized_keys | join('\n') }}"
    state: present
    exclusive: true

- name: Passwortloses sudo für Deploy-User entfernen
  ansible.builtin.file:
    path: "/etc/sudoers.d/90-{{ deploy_user }}"
    state: absent
  when: not deploy_user_sudo_nopasswd

- name: SSH-Passwort-Authentifizierung deaktivieren
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: "^#?PasswordAuthentication\\s+"
    line: "PasswordAuthentication no"
    state: present
  notify: restart ssh

- name: Root-SSH-Login deaktivieren
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: "^#?PermitRootLogin\\s+"
    line: "PermitRootLogin no"
    state: present
  when: disable_root_ssh
  notify: restart ssh

- name: Fail2ban aktivieren
  ansible.builtin.service:
    name: fail2ban
    state: started
    enabled: true
