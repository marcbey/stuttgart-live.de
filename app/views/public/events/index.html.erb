<% content_for :title, "Stuttgart Live - Konzerte" %>

<section class="public-shell">
  <header class="hero-header">
    <div class="hero-mark">stuttgart live</div>
    <h1>Konzerte in Stuttgart</h1>
    <p>Chronologisch kuratiert, direkt mit Ticket-CTA.</p>
    <div class="public-filter-toggle">
      <%= link_to "Alle Events",
                  events_path(filter: "all"),
                  class: "status-chip #{'status-chip-active' if @public_filter == 'all'}" %>
      <%= link_to "Nur SKS",
                  events_path(filter: "sks"),
                  class: "status-chip #{'status-chip-active' if @public_filter == 'sks'}" %>
    </div>
  </header>

  <div id="event-grid" class="flyer-grid">
    <% @events.each_with_index do |event, index| %>
      <% card_slot = (index % 5).zero? ? :grid_tall : :grid_default %>
      <%= render "event_card", event: event, card_slot: card_slot, public_filter: @public_filter %>
    <% end %>
  </div>

  <%= render "pagination", next_page: @next_page, public_filter: @public_filter %>
</section>
