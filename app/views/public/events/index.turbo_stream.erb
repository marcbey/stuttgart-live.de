<%= turbo_stream.replace "event-grid" do %>
  <%= render "event_cards",
             events: @events,
             browse_state: @browse_state,
             container_id: "event-grid",
             wrap: false %>
<% end %>
