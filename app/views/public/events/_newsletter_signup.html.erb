<section class="newsletter-signup-section">
  <div class="newsletter-signup-copy">
    <p class="eyebrow">Newsletter</p>
    <h2>Nichts verpassen</h2>
    <p>Neue Konzerte, Highlights und Vorverkäufe direkt in dein Postfach.</p>
  </div>

  <%= form_with model: NewsletterSubscriber.new, url: newsletter_subscribers_path, method: :post, local: true, class: "newsletter-signup-form" do |form| %>
    <%= hidden_field_tag :filter, browse_state.filter %>
    <%= hidden_field_tag :view, browse_state.route_params[:view] %>
    <%= hidden_field_tag :event_date, browse_state.event_date_param %>
    <%= hidden_field_tag :q, browse_state.query %>
    <%= form.label :email, "E-Mail-Adresse", class: "sr-only" %>
    <%= form.email_field :email,
                         class: "form-input newsletter-signup-input",
                         placeholder: "E-Mail-Adresse",
                         required: true,
                         autocomplete: "email" %>
    <%= form.submit "Newsletter abonnieren", class: "button newsletter-signup-button" %>
  <% end %>
</section>
