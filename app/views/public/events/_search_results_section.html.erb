<%
  result_count = events.size
  result_label = result_count == 1 ? "1 Ergebnis" : "#{result_count} Ergebnisse"
  search_meta = "Suche nach „#{browse_state.query}“ · #{result_label}"
%>

<section class="home-slider-section search-results-section">
  <%= render "home_slider_header",
             title: "Suchergebnisse",
             variant: :search,
             meta: search_meta %>
  <% if events.any? %>
    <%= render "event_rows", events: events, browse_state: browse_state, container_id: "event-grid", row_dom_id_prefix: :search_card, show_sold_out_ribbon: true %>
  <% else %>
    <div class="search-results-empty">
      <h2>Sorry, nix gefunden</h2>
      <p>Zu „<%= browse_state.query %>“ haben wir aktuell keine Events gefunden.</p>
    </div>
  <% end %>
</section>
